name: risky
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            .env
            dist
          key: node-modules
          restore-keys: |
            ${{ runner.os }}-
